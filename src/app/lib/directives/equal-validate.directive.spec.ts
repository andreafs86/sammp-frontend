import { EqualValidateDirective } from './equal-validate.directive';

describe('EqualValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualValidateDirective();
    expect(directive).toBeTruthy();
  });
});
